search {
	objects ^(?:\.{1,2}\/)?(?:.*obj.*/)(.*\.o)$
	inc .*\/inc\/(.*)\.h$
	src .*\/src\/(.*)\.c$
	include_pattern (?m)(?:^#include[\s]*)[<|"](.*)[>|"]
}

CC      = clang
dep     = -I../MasterThesis/code/inc
FLAGS   = -Wall
FLAGS  += $dep
Entry   = .

all: stuff build
	echo "Hello World!"
	echo $CC $FLAGS

crawl:
	echo $CC

stuff:
	echo "stuff"
	$FLAGS += -DDEBUG

../MasterThesis/code/obj/%.o:%.c
	echo $@
	echo $<
	$CC $FLAGS -c -o $@ $< 
